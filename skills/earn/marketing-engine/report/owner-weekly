#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "$0")/../../../.." && pwd)"
PYTHON="${LIFE_MANAGER_PYTHON:-$(command -v python3)}"
STATE_ROOT="${MARKETING_ENGINE_STATE_ROOT:-$HOME/.local/state/life-manager/marketing-engine}"

exec "$PYTHON" "$ROOT/skills/earn/marketing-engine/report/owner_report_cli.py" \
  sweep \
  --kind portfolio_weekly \
  --state-root "$STATE_ROOT" \
  "$@"
